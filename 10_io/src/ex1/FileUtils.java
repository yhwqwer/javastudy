
package ex1;

public class FileUtils {

  // 파일 복사 메소드
  public static void copy(String srcDir, String srcFile, String destDir, String destFile) {

    // 1. 원본 파일을 읽어서 byte[] 배열에 저장하기
    
    // 2. byte[] 배열에 저장된 데이터를 목적 파일로 만들기
    
  }
  
  // 파일 이동 메소드
  public static void move(String srcDir, String srcFile, String destDir, String destFile) {

    // 1. 파일 복사
    
    // 2. 원본 파일 삭제
    
  }

}
